import React from 'react';

const Title = (props) =>{
    
       return(
            <div class="header-text center u-pb-28">
                    {props.children}
            </div>
       )
    
}

export default Title;